
// combining arrays 

/*
const first = [1,2,3];
const second = [4,5,6];

//const combined = first.concat(second);

// with spread opeartor;
const combined = [...first, 'a', ...second, 'b'];
// combined and added new elements.

// cloning an array with spread operator
const clone = [...first];
*/




// combining objects

const first = {name: "Soner"};
const second = {job: "instructor"};

//const combined = first.concat(second);

// with spread opeartor;
const combined = {...first, ...second, location: "TÜRKİYEEEY"};
// combined and added new elements.

// cloning an array with spread operator
const clone = {...first};

